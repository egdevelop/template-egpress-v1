---
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, pubDate, author, category, featuredImage } = post.data;

function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', {
    day: 'numeric',
    month: 'short',
    year: 'numeric'
  });
}

function getReadingTime(content: string): number {
  const wordsPerMinute = 200;
  const words = content.split(/\s+/).length;
  return Math.ceil(words / wordsPerMinute);
}

const readingTime = getReadingTime(post.body || '');
---

<article class="bg-white rounded-2xl overflow-hidden shadow-lg">
  <div class="grid md:grid-cols-2 gap-0">
    <a href={`/blog/${post.id}`} class="block relative overflow-hidden">
      <div class="h-64 md:h-full min-h-[300px]">
        {featuredImage ? (
          <img 
            src={featuredImage}
            alt={title}
            class="w-full h-full object-cover transition-transform duration-500 hover:scale-105"
            loading="eager"
            width="600"
            height="400"
          />
        ) : (
          <div class="w-full h-full bg-gradient-to-br from-rose-100 to-pink-200 flex items-center justify-center">
            <svg class="w-24 h-24 text-rose-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
        )}
      </div>
    </a>
    
    <div class="p-8 md:p-10 flex flex-col justify-center">
      <div class="flex items-center gap-3 mb-4">
        <span class={`tag tag-${category}`}>{category.replace('-', ' ')}</span>
        <span class="text-gray-400">•</span>
        <span class="text-sm text-gray-500">{readingTime} min read</span>
      </div>
      
      <a href={`/blog/${post.id}`}>
        <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4 hover:text-rose-600 transition-colors leading-tight">
          {title}
        </h2>
      </a>
      
      <p class="text-gray-600 mb-6 line-clamp-3">
        {description}
      </p>
      
      <div class="flex items-center gap-3 mt-auto">
        <img 
          src={author.avatar || `https://ui-avatars.com/api/?name=${encodeURIComponent(author.name)}&background=E11D48&color=fff&size=40`}
          alt={author.name}
          class="w-10 h-10 rounded-full"
          loading="lazy"
          width="40"
          height="40"
        />
        <div>
          <p class="font-medium text-gray-900">{author.name}</p>
          <p class="text-sm text-gray-500">{formatDate(pubDate)}</p>
        </div>
      </div>
    </div>
  </div>
</article>
